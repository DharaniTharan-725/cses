#include<bits/stdc++.h>
using namespace std;
string solve(int a,int b)
{
    if((2*b-a)%3 || (2*b-a)<0 ||(2*a-b)%3 || (2*a-b)<0)
    return "NO";
    return "YES";
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int a,b;
        cin>>a>>b;
        cout<<solve(a,b)<<endl;
    }
}
