#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cin>>n;
	long long arr[n];
	set<int>unique;
	for(int i=0;i<n;i++)
	{
	        cin>>arr[i];
	        unique.insert(arr[i]);
	}
	cout<<unique.size()<<endl;
}
